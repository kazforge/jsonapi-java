/**
 * Jackson-major-neutral mapping implementation helpers.
 *
 * <p>This package is an unsupported implementation namespace shipped inside the Jackson API
 * artifact so the Jackson 2 and Jackson 3 adapters can share neutral mapping roles, diagnostics,
 * and identifier-meta state. Its Java-public types are adapter implementation details, not
 * supported public API, and must not appear in supported public signatures.
 */
@NullMarked
package com.kazforge.jsonapi.jackson.internal.mapping;

import org.jspecify.annotations.NullMarked;
