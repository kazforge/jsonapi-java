/**
 * Passive, application-shaped carriers for first-class local-identifier ({@code lid}) mapping:
 * domain shapes declaring {@code @JsonApiId}, {@code @JsonApiLocalId}, or both as independent
 * identity roles.
 *
 * <p>This package contains passive carriers only. Scenario catalogs, descriptors, resource loading,
 * invariants, and other executable support remain outside this package.
 */
package com.kazforge.jsonapi.fixtures.localid;
